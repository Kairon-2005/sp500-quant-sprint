# Pinned to the versions actually used (Python 3.14). See data/metadata/
# data_snapshot.json for the exact fetch provenance of each downloaded file.

# ---- Core data stack ----
pandas>=3.0,<4
numpy>=2.5,<3
pyarrow>=24,<25         # Parquet columnar storage (efficient for large panels)

# ---- Data sources ----
yfinance>=1.5,<2        # primary: Yahoo Finance OHLCV (raw + adj + actions)
curl_cffi>=0.15         # browser impersonation for yfinance (mitigates HTTP 429)
pandas-datareader>=0.11 # fallback source: Stooq EOD data
requests>=2.31
lxml>=5.0               # pandas.read_html (S&P 500 constituents from Wikipedia)
beautifulsoup4>=4.12
html5lib>=1.1

# ---- Calendars / config / utils ----
pandas-market-calendars>=5.0   # authoritative NYSE (XNYS) trading calendar
PyYAML>=6.0
tqdm>=4.66

# ---- Analysis & viz (weeks 1.2 / 1.3) ----
matplotlib>=3.8
seaborn>=0.13

# ---- Testing ----
pytest>=8.0
tabulate>=0.9        # pandas .to_markdown() for report tables
